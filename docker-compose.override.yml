services:
  backend:
    ports: !override
      - '127.0.0.1:3010:3000'

  frontend:
    build:
      args:
        VITE_API_URL: http://localhost:3010/api
    ports: !override
      - '127.0.0.1:8090:80'
